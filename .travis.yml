language: cpp
compiler:
    - clang
sudo: false
addons:
  apt:
    packages:
      - libboost1.55-dev
      - libssl-dev
      - libcurl4-openssl-dev
script: scons install apps samples
branches:
    only:
        - master
notifications:
    email:
        on_success: change
        on_failure: always
